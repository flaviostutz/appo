# deployments module Makefile — Infrastructure-as-code (one sub-dir per deployment group)

.PHONY: build lint test lint-fix

build:
	@echo "[deployments] build — not yet configured"

lint:
	@echo "[deployments] lint — not yet configured"

test:
	@echo "[deployments] test — not yet configured"

lint-fix:
	@echo "[deployments] lint-fix — not yet configured"
