# appo module Makefile — Agentic AI (Python, LangGraph)

.PHONY: build lint test lint-fix

build:
	@echo "[appo] build — not yet configured"

lint:
	@echo "[appo] lint — not yet configured"

test:
	@echo "[appo] test — not yet configured"

lint-fix:
	@echo "[appo] lint-fix — not yet configured"
