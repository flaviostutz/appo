# shared module Makefile — Libraries shared across modules

.PHONY: build lint test lint-fix

build:
	@echo "[shared] build — not yet configured"

lint:
	@echo "[shared] lint — not yet configured"

test:
	@echo "[shared] test — not yet configured"

lint-fix:
	@echo "[shared] lint-fix — not yet configured"
