# devices module Makefile — Hardware and firmware (one sub-dir per device type)

.PHONY: build lint test lint-fix

build:
	@echo "[devices] build — not yet configured"

lint:
	@echo "[devices] lint — not yet configured"

test:
	@echo "[devices] test — not yet configured"

lint-fix:
	@echo "[devices] lint-fix — not yet configured"
